[project]
name = "esp32"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "esptool>=5.1.0",
]

[tool.uv.workspace]
members = [
    "ESP32-S3-PhotoPainter",
]
